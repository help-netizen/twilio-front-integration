# Bugs Report

| # | Описание | Где воспроизводится | Доп. инфо | Статус |
|---|----------|----------------------|-----------|--------|
| 16 | В визарде Convert to Job форма Custom Time показывает "No technicians found", хотя техники доступны (форма Available Timeslots корректно показывает слоты на ту же дату). Ожидание: Custom Time должна отображать список доступных техников | Convert to Job wizard → Custom Time (прод) | Причина: route team-members единственный использовал per-tenant Zenbooker client (getClientForCompany) вместо глобального; на проде per-tenant ключ из companies.zenbooker_api_key не работает. Frontend .catch(() => {}) молча глотал ошибку. Fix: убрал companyId из getTeamMembers() call (consistency с остальными routes), добавил error handling на фронте | исправлен |
| 15 | Инвертированные статусы звонков: принятый входящий звонок (1:20 разговор) показывает "No Answer", а непринятый (voicemail) — "Completed". Ожидание: принятый звонок = Completed, непринятый = No Answer | Pulse / статусы звонков | Причины: (A) reconcileParentCall не находит winner когда child leg финализирован handleDialAction без duration_sec → ставит no-answer; (B) reconcileStaleCalls → fetchAndUpdateFromTwilio без guard перезаписывает voicemail_recording на completed. Fix: fallback winner detection, duration в child finalization, guard в fetchAndUpdateFromTwilio, stuck voicemail→no-answer | исправлен |
| 14 | Исходящий звонок с (877) 419-4983 на 5085140320 попадает на войсмейл «все заняты», как будто маршрут зацикливается на самого себя. Ожидание: исходящий звонок должен напрямую соединять с номером назначения, а не проходить через входящую маршрутизацию | Softphone / исходящие звонки | Номер отправителя: +18774194983, номер назначения: 5085140320. Похоже на петлю маршрутизации — звонок уходит обратно на inbound handler вместо прямого набора | открыт |
| 13 | Когда все операторы заняты (на звонке), новый входящий звонок сразу уходит на голосовую почту вместо ожидания в очереди. Ожидание: звонок должен вставать в очередь и ждать пока кто-нибудь из операторов освободится; оператору желательно видеть что есть звонок в ожидании | Softphone / входящие звонки (handleVoiceInbound) | Сценарий: единственный оператор на звонке → новый входящий → сразу автоответчик вместо hold-очереди | открыт |
| 12 | SSE EventSource уходит в бесконечный цикл переподключений с ошибкой ERR_HTTP2_PROTOCOL_ERROR, что приводит к массовому спаму сетевых запросов (сотни Fetch/XHR). Ожидание: SSE должен стабильно держать соединение или переподключаться с экспоненциальным backoff без спама запросов | Pulse / весь фронтенд (SSE endpoint /events/calls) | Причина: 9 независимых EventSource на вкладку (каждый useRealtimeEvents создавал свой). Fix: singleton SSE manager (sseManager.ts), один EventSource на всё приложение, экспоненциальный backoff до 60с с jitter, пауза при скрытии вкладки | исправлен |
| 11 | Очередь входящих звонков не работает корректно: если звонят два клиента одновременно и оператор отклоняет первый звонок, второй звонок не отображается. Ожидание: после отклонения/завершения первого звонка оператор должен видеть следующий ожидающий звонок из очереди | Softphone / входящие звонки | Причина: handleDialAction безусловно отправлял отклонённые звонки на войсмейл вместо переадресации обратно на handleVoiceInbound. Fix: redirect с dialAttempt счётчиком (до 3 попыток), уменьшение hold loop pause с 5s до 2s | исправлен |
| 10 | Иногда в таймлайне карточка контакта не загружается и не отображается — сразу идут сообщения/звонки без шапки контакта. Ожидание: карточка контакта должна всегда загружаться в middle column перед таймлайном | Pulse (/pulse) — таймлайн контакта | Ошибки в консоли: 401 на /api/voice/token, 404 на temporary-url | открыт |
| 9 | Пропущенные звонки сначала отображаются с красной иконкой (missed), но через несколько секунд иконка меняется на зелёную (completed). Ожидание: пропущенный звонок должен оставаться красным, чтобы операторы видели что звонок был пропущен | Pulse / таймлайн — карточки звонков | Причина: 3 незащищённых пути: (1) child-leg guard работал только для parent (!parentCallSid), (2) reconcileParentCall не защищал no-answer, (3) reconcile запускался даже при skipUpsert. Fix v2: guard для child+parent legs, no-answer в reconcile guard, skipUpsert check перед reconcile | исправлен |
| 8 | При выборе фильтра на странице Pulse бесконечно моргает "Loading more..." — система пытается подгружать список, даже если больше нет таймлайнов, соответствующих критериям фильтра. Ожидание: загрузка должна останавливаться, когда данных больше нет | Pulse (/pulse) — фильтрация таймлайнов | Бесконечный цикл запросов при пустом результате фильтра | открыт |
| 7 | При исходящем звонке после завершения предыдущего вызова (клиент отклонил или принял и завершил) повторный звонок на тот же номер блокируется ошибкой "A team member is already on the line with this contact. Please try again later." Предыдущий звонок зависает как активный. Ожидание: после завершения звонка система должна корректно освобождать контакт для повторного вызова | Softphone / исходящие звонки | Номер клиента: 7743831412. Ошибка: pre-flight busy check считает контакт занятым из-за stale записи предыдущего звонка | открыт |
| 6 | Входящий звонок попадает на войсмейл с сообщением "Hello, our team is currently assisting other customers", хотя все операторы свободны и не разговаривают с клиентами. Ожидание: звонок должен маршрутизироваться на свободных операторов | Входящие звонки / маршрутизация (twilioWebhooks.js → handleVoiceInbound) | Последний случай: звонок от 5085140320. Причина: stale записи в таблице calls. Fix: 4 слоя защиты (age filter, Twilio API fallback, child leg finalization, reduced stale threshold) | исправлен |
| 5 | Выпадающий список Quick Messages не скроллится — при большом количестве сообщений меню выходит за пределы экрана без возможности прокрутки. Ожидание: dropdown должен иметь max-height и scroll | Pulse → SMS форма → кнопка "Quick" | Воспроизводится на проде с большим списком quick messages | открыт |
| 4 | Работа /jobs/282 не связана с контактом /contacts/867, хотя должна быть. В Pulse timeline (/pulse/timeline/183) и в карточке контакта отображается только одна работа (/jobs/202) вместо двух. Ожидание: обе работы должны быть привязаны к контакту и отображаться в его списке работ | /pulse/timeline/183, /contacts/867, /jobs/282 | job id=282 имеет contact_id=NULL или неверный contact_id; job id=202 привязан корректно | открыт |
| 3 | При скачивании CSV-отчёта по работам вся сумма инвойса (включая неоплаченную часть) попадает в колонку Paid. Ожидание: Paid должен отражать только фактически оплаченную сумму | Jobs / CSV export | Колонка Paid в CSV = invoice_total вместо реально оплаченной суммы | открыт |
| 2 | Работа 914888 отсутствует в разделе Jobs, хотя присутствует в Zenbooker | Jobs / Zenbooker | https://zenbooker.com/app?view=jobs&view-job=1771521199420x206863333962022900 | закрыт |
| 1 | Входящее SMS не появляется в таймлайне контакта в реальном времени — пуш-уведомление приходит, но сообщение видно только после перезагрузки страницы. Ожидание: новые события (SMS) должны появляться в таймлайне по SSE без перезагрузки | /pulse/timeline/{contactId} | SSE не доставляет событие SMS в таймлайн | закрыт |
