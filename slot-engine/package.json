{
  "name": "albusto-slot-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Smart Time Slot Recommendation Engine (SLOT-ENGINE-001) — standalone, stateless service that ranks arrival time-frames + technician for a new job. MVP: haversine travel, single-technician, fixed candidate windows.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "express": "^4.19.2"
  }
}
