# Production Deploy — 2026-06-13 (migrations 096–104 + app code)

Target: Vultr `deploy@108.61.87.117`, `/opt/albusto` (docker compose:
`albusto-app`, `albusto-postgres-1`, `albusto-keycloak-1`). App on
127.0.0.1:3000 behind nginx (app/api/auth.albusto.com). Fly kept only for /pulse.

## Scope
17 branch commits + uncommitted BILLING-UI / ARM-001 / PAY-CONS-001. DB
migrations 096→104 (prod is at pre-096 baseline; none of 096–104 applied).
Only destructive op in the set: `DROP TABLE IF EXISTS payments` (0 rows, 0 live
code refs). Everything else additive (`IF NOT EXISTS`). Migrations were tested
idempotently on prod-schema/-data copies during the session.

## Order (additive-first, then code)
### Phase A — Git
1. Run test suite (expect 699 pass / 22 pre-existing fail).
2. Commit uncommitted work on the feature branch.
3. Merge feature branch → `master`.
4. Push `master` to origin (no CI → no auto-deploy).

### Phase B — DB migration
5. Fresh prod backup: `pg_dump albusto` → `/opt/albusto/dumps/` + download a copy.
6. Apply 096→104 in order via `psql -v ON_ERROR_STOP=1 -f`.
7. Verify: signature objects exist; payments parity (ledger == zb succeeded);
   billing plans seeded; automation tables present.

### Phase C — App deploy
8. Sync updated code into `/opt/albusto/app`.
9. Rebuild `albusto-app` image; `docker compose up -d app`.
10. Smoke: `/health`, login redirect, `/api/billing` (auth), `/settings/billing`.

## Rollback
- DB: `psql albusto < <backup dump>` (taken in step 5).
- App: previous image is cached by docker (`sha256:191099c3…`) — re-tag + `compose up -d`.
- Git: `git revert` the merge if needed.

## Execution log (2026-06-13)
- **Phase A** ✅ committed `e4c7574`; master fast-forwarded `9a81418..e4c7574`; pushed.
- **Phase B** ✅ backup `predeploy_20260613_000334.sql` (394M, marker OK) + off-server
  gz copy. Migrations 096→104 applied in order, all ✅. Verified: signature tables,
  096 cols/indexes, `payments` dropped, billing plans seeded with `included_units`,
  `is_system` col, event/automation tables. PAY-CONS parity on live prod: 1027 rows,
  ledger total = zb succeeded total = $197,253.26.
- **Phase C** ✅ all feature flags enabled in `.env` (`.env.bak_predeploy_20260613`):
  SELF_SIGNUP, SMS_2FA, RULES_ENGINE_AR, AGENT_WORKER, LEADS_TAB, BILLING_PROVIDER=stripe.
  Seeded default AR system rules for all companies (closes RULES_ENGINE_AR gap).
  rsync code → rebuild `albusto-app` → `compose up -d app`. Smoke: app 200, /health
  healthy, auth OIDC 200, /api/billing unauth 401, authed SSE session live.
- **Hotfix during deploy:** agent worker logged `tasks.updated_at does not exist`
  (migration 100's `ALTER TABLE tasks` omitted it). Added **migration 105**
  (`tasks.updated_at`), applied to prod, error cleared. Verified create_task path:
  all columns + `uq_tasks_one_open_per_thread` present.

## Post-deploy: prod migration-gap restoration (2026-06-13)
The Vultr prod DB was restored from a **partial dump** missing several pre-096
feature migrations (baseline `schema_pre_096` itself lacked them). Surfaced when
`/settings/document-templates` returned 500 (`relation "document_templates" does
not exist`). Audited all migration tables vs prod and restored the gaps
(idempotent — `IF NOT EXISTS` / `ON CONFLICT`):
- **084 + 086** — `document_templates` (+ invoice type, seeded 2 templates).
- **085** — `estimate_item_presets`.
- **088 + 089 + 090** — Sales/CRM suite (11 `crm_*` tables, CRM cols on `tasks`,
  pipeline weekly snapshots).
- **087** — `invoices.public_token` column + unique index.
- **087/088 seeds** — mail-secretary + VAPI marketplace apps.

Post-fix audit: **zero remaining table gaps** (only `twilio_webhook_inbox`, a
false positive — renamed to `webhook_inbox`, which exists). Column/seed-level
gaps in pre-existing tables could still exist (audit was table-level); they'll
surface as specific `column … does not exist` errors and be patched the same way.
NOTE: do NOT run `apply_migrations.js` wholesale on prod — data migrations like
`078_migrate_job_comments_to_notes` and `025_contact_dedupe` would duplicate
data on re-run. Use a prod-data clone to discover gaps, then apply additive ones.

## Pending (owner action)
- **Comprehensive migration audit (recommended):** run `apply_migrations.js`
  against a clone of prod to enumerate every remaining gap safely, then apply
  only the additive ones.
- **Stripe live billing:** add `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` to
  `/opt/albusto/.env` and `docker compose up -d app` — billing activates with no
  code change (currently degraded-mode: cabinet renders, checkout returns 422).
- Configure the Stripe webhook endpoint → `https://api.albusto.com/api/billing/webhook`.
