# MODAL-REDESIGN-001 — Аудит и редизайн модальных окон

> UX/дизайн-документ. Не углубляется в техническую реализацию — описывает реестр,
> классификацию, единые правила, концепцию и рекомендации. Привязан к существующей
> дизайн-системе **Blanc** и к уже имеющимся паттернам (`Dialog`, `FloatingDetailPanel`).

## 1. Краткий вывод

В системе ~44 модальных окна. Все формы рендерятся через один общий компонент
`components/ui/dialog.tsx`, у которого десктопный размер по умолчанию — **`max-w-lg` (512px)**
по центру экрана. Это и есть корень проблемы: многополевые формы ужимаются в маленький
центральный блок, элементы мельчают, появляется внутренний скролл.

При этом в системе **уже есть** правильный широкий формат — `FloatingDetailPanel`
(боковая «плавающая» панель с вариантом `--wide`), который используется для детальных
карточек (Job, Estimate, Lead, Contact …). То есть нужный паттерн существует — его надо
обобщить на формы, а центральный `Dialog` дополнить размерными вариантами.

**Решение в одну фразу:** ввести размерные варианты у общего `DialogContent`
(`sm` / `default` / `wide` / `full`) и перевести формы из «маленького центрального окна»
в «широкий слой / боковую панель / полноэкранный сценарий» по единому правилу; простые
подтверждения оставить компактными.

## 2. Единые правила (rule → формат)

| Тип окна | Признак | Формат | Размер (desktop) |
|---|---|---|---|
| **Подтверждение** | короткий текст + 1–2 кнопки, 0 полей | компактный центр (как есть) | `sm` ≈ `max-w-md` (448px) |
| **Короткая форма** | 2–4 поля | широкий центр, поля крупнее, без скролла | `wide` ≈ `max-w-2xl` (672px) |
| **Форма с секциями** | 5+ полей / блоки | широкий слой, 2 колонки, без скролла | `wide`/`full` ≈ `max-w-3xl–4xl` |
| **Сложный сценарий** | мастер, 10+ полей, документ (Estimate/Invoice), важное решение | полноэкранный слой или боковая панель | `full` (≈ 90–96% экрана) |
| **Длинный read-only контент** | превью, история, список | боковая панель `FloatingDetailPanel` | `--wide` |

Правила-инварианты (раздел 8 ТЗ):
- использовать бóльшую часть экрана, минимизировать/убрать внутренний скролл;
- поля/кнопки/типографика — в масштабе основных экранов (не мельче);
- единообразие однотипных сценариев; визуально — часть современного интерфейса, не «старый слой».

## 3. Концепция нового отображения

Три формата, все на токенах Blanc (radius 16/22/28, `--blanc-surface-strong`, `--blanc-line`,
IBM Plex Sans / Manrope, eyebrow-заголовки):

1. **Wide form layer** — расширенный центральный слой (`Dialog size="wide"`/`"full"`).
   Для форм. Шапка (заголовок + краткое описание), тело с **2-колоночной** сеткой и
   логическими блоками (как в `NewJobDialog`: Contact · Address · …), липкий футер с
   действиями. Цель — вся форма видна без внутреннего скролла на типовом экране.
2. **Side panel** — `FloatingDetailPanel` (`wide`). Для детальных карточек и длинного
   read-only контента (превью документа, история версий). Уже используется — распространяем.
3. **Page scenario** — для самых тяжёлых сценариев (редактор Estimate/Invoice как документ,
   мастер конвертации) лучше выделенная страница/маршрут, а не модалка.

Реализуется централизованно: добавить проп `size` в `DialogContent` (один источник правды),
а не править `max-w-*` в каждом окне вручную. После этого редизайн окна = смена `size` +
раскладка тела в 2 колонки/блоки.

## 4. Реестр модальных окон и классификация

Легенда: **R** — редизайн, **K** — оставить компактным (подтверждение), **P** — уже широкая
панель (паттерн-эталон, оставить). «Поля» — кол-во контролов ввода (объективный замер).

### 4.1 Редизайн — формы (R)

| Окно | Поля | Сейчас | → Формат | Почему |
|---|---|---|---|---|
| `estimates/EstimateEditorDialog` | 20 | `max-w-xl` + scroll | **full / page** | документ-редактор, длинная форма, скролл |
| `invoices/InvoiceEditorDialog` | 20 | `max-w-xl` + scroll | **full / page** | то же |
| `leads/CreateLeadDialog` | 12 | `max-w-[560px]` + scroll | **wide (2-col)** | многополевая форма, скролл |
| `pages/CompanyUserDialogs` | 12 | `max-w-xl` | **wide (2-col)** | создание/редактирование пользователя |
| `leads/EditLeadDialog` | 11 | `max-w-2xl` + scroll | **wide (2-col)** | многополевая, скролл |
| `transactions/RecordPaymentDialog` | 9 | `max-w-md` | **wide** | 9 полей зажаты в 448px |
| `contacts/EditContactDialog` | 7 | `max-w-lg` + scroll | **wide (2-col)** | многополевая, скролл |
| `estimates/EstimateItemDialog` | 7 | `max-w-lg` | **wide** | позиция сметы, много полей |
| `jobs/NewJobDialog` | 6 | `max-w-lg` + scroll | **wide (2-col)** | 4 блока, при заполнении скролл |
| `super-admin/CreateCompanyDialog` | 6 | `max-w-[425px]` | **wide** | 6 полей в 425px |
| `schedule/DispatchSettingsDialog` | 5 | `max-w-md` | **wide** | настройки, мелкие поля |
| `super-admin/BootstrapAdminDialog` | 4 | `max-w-[425px]` | **wide** | форма админа |
| `leads/ConvertToJobDialog` | мастер | `max-w-xl` + scroll | **full / page** | пошаговый сценарий |
| `transactions/RefundDialog` | 3 | `max-w-md` | **wide-light** | сумма+причина, чуть крупнее |
| `workflows/ActionsBlock` | 3 | `max-w-md` | **wide** | конфигурация действия |
| `invoices/ManualCardDialog` | Stripe | — | **wide-light** | платёжная форма (Stripe Elements) |
| `schedule/NewJobModal` | 2 | `max-w-md` | **wide-light** | заголовок+адрес из слота |
| `pages/IntegrationDialogs` / `IntegrationsPage` | 1–2 | `max-w-lg/xl` | **wide-light** | настройки интеграций |
| `pages/VapiSettingsPage` (dlg) | 3 | — | **wide** | настройки VAPI |
| `pages/StripePaymentsSettingsPage` (dlg) | — | `max-w-3xl` | проверить | настройки оплат |
| `pages/ServiceTerritoriesPage` (dlg) | 7 | `max-w-5xl` | **оставить wide** | уже широкий — привести к стандарту |

### 4.2 Уже широкая панель — эталон (P, оставить)

`jobs/JobDetailPanel`, `estimates/EstimateDetailPanel`, `leads/LeadFinancialsTab`,
`jobs/JobFinancialsTab`, и страницы, монтирующие `FloatingDetailPanel`: `JobsPage`,
`LeadsPage`, `ContactsPage`, `PaymentsPage`, `TransactionsPage`, `EstimatesPage`,
`InvoicesPage`, `SchedulePage`. Это целевой паттерн — менять не нужно, на него равняемся.
`conversations/CustomTimeModal` (`max-w-5xl`, выбор слота) — уже широкий, оставить (полировка).

### 4.3 Оставить компактным — подтверждения (K)

| Окно | Почему оставить |
|---|---|
| `workflows/PublishDialog` | публикация — подтверждение |
| `super-admin/ResetPasswordDialog` | подтверждение/одно поле |
| `estimates/EstimateSendDialog` | отправка — подтверждение |
| `layout/AppLayout` (dlg) | запрос разрешения на уведомления |
| `leads/ColumnSettingsDialog` | переключатели колонок (короткий список) |
| `workflows/VersionHistory` | короткий read-list (если вырастет → panel) |
| `estimates/EstimateSummaryDialog`, `EstimatePreviewDialog` | read-only сводка/превью (превью → panel при длинном контенте) |

## 5. Обоснование классификации

- **R** — у окна ≥2 полей ввода и/или длинная форма / внутренний скролл / зажатый размер
  (`max-w-md/lg`) → восприятие страдает, элементы мелкие. Перевод в `wide`/`full`/panel
  убирает скролл, укрупняет элементы, даёт обзор формы целиком.
- **P** — окно уже использует широкую боковую панель: целевой паттерн, не трогаем.
- **K** — нет формы, только короткий текст + кнопки: компактный центр уместен, UX-проблем нет.

## 6. Рекомендации на будущее (как проектировать новые окна)

1. **Считай поля.** 0 полей + подтверждение → `Dialog size="sm"`. 2+ полей → минимум `wide`.
2. **Не зажимай форму.** Запрещено `max-w-md/lg` для многополевых форм — используй `size`-проп,
   а не ручной `max-w-*` в каждом окне.
3. **Нет внутреннему скроллу.** Если форма не влезает без скролла — это `wide`/`full` или
   2 колонки, или это уже не модалка, а **страница**.
4. **Документ — это страница/панель,** а не модалка (Estimate/Invoice editor, мастера).
5. **Масштаб как на основных экранах** — те же размеры полей/кнопок/типографики (без «мелкого слоя»).
6. **Один источник правды** — размер задаётся `size`-пропом `DialogContent`, дизайн-токенами Blanc;
   не плодить кастомные обёртки.
7. **Единообразие** — однотипные сценарии (все create/edit-формы) — один формат.

## 7. Что дальше (внедрение)

Документ — это deliverables 1–5,7 (реестр, классификация, обоснование, правила, концепция,
рекомендации) + макеты (см. ниже, deliverable 6). Внедрение предлагается так:
1. Добавить `size` в `components/ui/dialog.tsx` (`sm|default|wide|full`) — централизованно.
2. Прогнать список **R** пачками (формы → `wide`, тело в 2 колонки/блоки, липкий футер).
3. Тяжёлые редакторы (Estimate/Invoice) — вынести в страницу/боковую панель.
4. **K** не трогать.
