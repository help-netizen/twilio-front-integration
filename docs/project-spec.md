# Blanc Contact Center — Project Spec

> Обзор проекта, стек технологий, деплой, интеграции.

---

## Estimates

Estimates are client-facing repair documents created from Lead or Job financial contexts. The canonical display number is `ESTIMATE L-{leadNumber}-{sequence}`; for Job-created estimates, the sequence is scoped to the Job. The estimate UUID/database id is stable; the display number may change when the estimate is linked to a different Lead/Job context.

Core storage is relational PostgreSQL, not XML. Estimate rows hold document-level fields such as Summary, tax rate, discount type/value, archive metadata, signature requirement, and approved snapshot. Estimate items are generic custom rows with future-compatible Price Book references; item type/category may exist internally but is not shown in the estimate composer.

Lifecycle:
- Active statuses: `draft`, `sent`, `viewed`, `approved`, `declined`
- P0 send is a workflow stub that asks for `Email` or `Text` and does not mutate status.
- Approve requires at least one item and stores an approved snapshot.
- Editing any non-draft estimate resets it to `draft`; editing an approved estimate preserves the approved version in history.
- Archive sets `archived_at`/`archived_by` without changing status. Archived estimates are read-only, hidden from public portal access, and visible internally only when `All` is selected.

Client-facing Preview/PDF content includes optional Summary, items, totals, tax as an aggregate line only, and Blanc default Terms & Warranty. Per-item taxable markers are internal UI only.

---

## External Integrations API

Token-authenticated HTTP surface mounted at `/api/v1/integrations`. All endpoints share the same auth chain (`rejectLegacyAuth → validateHeaders → authenticateIntegration → rateLimiter`) and the same error envelope `{ success, code, message, request_id }`.

| Method | Path | Scope | Purpose |
|--------|------|-------|---------|
| POST | `/leads` | `leads:create` | External lead ingestion (Workiz-compatible) |
| GET  | `/analytics/summary` | `analytics:read` | Ads funnel metrics for a period |
| GET  | `/analytics/calls` | `analytics:read` | Paged inbound calls to tracking DID |
| GET  | `/analytics/leads` | `analytics:read` | Paged leads in period + tracking-call attribution |
| GET  | `/analytics/jobs` | `analytics:read` | Paged jobs linked to period's leads |

Keys are generated via:
- `backend/scripts/issue-analytics-key.js` for `analytics:read`
- the admin UI (`/api/admin/integrations`) for `leads:create`

Secrets are stored as `SHA-256(secret + BLANC_SERVER_PEPPER)` and never logged. Per-company isolation is enforced via `api_integrations.company_id` → `req.integrationCompanyId`.

---

## Twilio API Client (TWC-001)

All backend code obtains the Twilio Node SDK REST client via a single shared accessor: `getTwilioClient()` from `backend/src/services/twilioClient.js`. The first call reads `TWILIO_ACCOUNT_SID` and `TWILIO_AUTH_TOKEN` and constructs one `twilio(sid, token)` instance for the lifetime of the process; every subsequent caller reuses that instance and its underlying `https.Agent` keep-alive pool toward `api.twilio.com`.

Direct construction of a Twilio REST client (`twilio(sid, token)`) inside service or route functions is forbidden — a regression test enforces this for the four historical hot-spots (`reconcileStale`, `callAvailability`, `inboxWorker`, `phoneSettings`). Static helpers (`twilio.validateRequest`, `twilio.jwt.AccessToken`) are unaffected and continue to be used directly by webhook signature validators and the voice token route.

---

## Stripe Payments — Tenant Customer Payments (F018 / STRIPE-PAY-001, Phases 1–5 + STRIPE-ADHOC-PAY-001)

A marketplace integration that lets each tenant company connect its own Stripe
account (Stripe Connect, direct charges, tenant = merchant of record, no application
fee) and collect customer payments. Strictly separate from platform subscription
billing (ADR-001): a dedicated `stripeConnectProvider` and a distinct webhook secret
(`STRIPE_CONNECT_WEBHOOK_SECRET`).

**Setup:** marketplace card `stripe-payments` → `/settings/integrations/stripe-payments`
(`tenant.integrations.manage`). A readiness state machine (`not_connected` →
`onboarding_incomplete` → `action_required` → `payments_disabled` → `payouts_disabled`
→ `connected_ready`) gates online collection; status is refreshed from Stripe via
`refresh-status` and `account.updated` webhooks.

**Collection (Phases 1–2, invoice-scoped):** authorized users create/reuse a Stripe
Checkout payment link per invoice (`payments.collect_online`); public customers pay via
an opaque-token Pay-now endpoint. Successful payments are written once to the canonical
`payment_transactions` ledger (`external_source='stripe'`) through
`paymentsService.createTransaction`, with invoice paid/balance updated via the canonical
invoice path.

**Phases 3–5 (done, 2026-06-14):** manual card entry via Stripe **Payment Element**
(`payments.collect_keyed`, `ManualCardDialog`); **Tap to Pay / Terminal** — full backend
(`connection-token`, `card_present` payment-intent, cancel; `payments.collect_terminal`),
the on-device NFC UI awaiting the native mobile shell (MOBILE-TECH-APP v1.5/M12);
**refunds** (`POST /api/payments/:id/stripe-refund`, `payments.refund`, idempotent
`applyStripeRefund`) and **dispute** handling (`charge.refunded` / `charge.dispute.created`
webhooks). Migrations 111–112 (`stripe_terminal_locations`, collect_keyed/terminal perms).

**Phase-independent ad-hoc collection (STRIPE-ADHOC-PAY-001, 2026-07-07):** a
**Collect payment** entry point on the Job card → Finance tab, invoice-INDEPENDENT and
**arbitrary-amount**. Enter any amount (min $0.50 / max $100k, `assertAdhocAmount`) →
charge card manually, **send a payment link** (email via `emailService.sendEmail` + SMS
via `conversationsService.sendMessage`, mirroring `sendInvoice`), or copy the link. The
ad-hoc link is a Stripe-hosted Checkout that redirects to a static `/pay/thanks` page; the
payment lands in `payment_transactions` with `job_id` and **no invoice** (the existing
webhook resolves `job_id` from session metadata — no ledger/webhook change). Reuses the
`checkout_link` surface (`invoice_id` NULL, `job_id` set); idempotent per
`job-${companyId}-${jobId}-${amount}`. When Stripe isn't `connected_ready` the button is
replaced by a Connect-Stripe CTA (or "ask an admin" for non-`tenant.integrations.manage`
users). No migration.

**Connect-flow UX (STRIPE-CONNECT-UX-001, 2026-07-10):** frontend-only redesign of the
connect journey. The not-connected settings page leads with a violet-cloud hero ("Get paid
on the spot") plus in-product pricing and trust signals: rate chips (2.9% + 30¢ · $0 monthly
· 0% added by Albusto), a **What it costs** table (hardcoded Stripe US rates, footnoted to
stripe.com/pricing), and "Powered by Stripe · Card data never touches Albusto"; a partial
connection shows a compact "Almost there" cloud that absorbs Resume onboarding, and
checklist/env copy was softened from Stripe-speak to plain labels. The Job → Finance
Connect-Stripe CTA (from STRIPE-ADHOC-PAY-001) is now a light cloud banner with three
states — connect / finish-setup / ask-admin (locked). The cloud itself is a reusable
pure-CSS pattern (`.blanc-cloud` + `ui/CloudBanner`, layered radial gradients, no images)
shared by both call-sites. Collection gating and payment logic are byte-identical —
`connected_ready` still gates everything exactly as above.

**Storage:** `stripe_connected_accounts` (one per company), `stripe_payment_sessions`,
`stripe_webhook_events`. Idempotency is enforced at two layers: per Stripe event id
(`stripe_webhook_events` unique) and per payment (`payment_transactions(company_id,
external_id) WHERE external_source='stripe'`). Webhook objects are tenant-scoped by
resolving the connected-account id to a company before any ledger mutation; unknown
accounts are rejected without mutation. Card data never touches Albusto (Stripe-hosted
Checkout only); secrets live in env, not tenant metadata.

**Out of scope:** on-device Tap-to-Pay NFC UI (needs the native mobile shell — v1.5/M12),
expanded reporting filters, application fees.

---

## Document Templates (F015)

Per-company, versioned, JSON-encoded descriptors that drive client-facing document rendering. P0 covers `document_type='estimate'`; the registry, factory, and Settings UI are designed so adding `invoice` and `work_order` is a data-only follow-up.

**Storage:** `document_templates(id, company_id, document_type, name, slug, is_default, schema_version, content JSONB, archived_at, created_*, updated_*)`. Unique partial index ensures exactly one active default per `(company_id, document_type)`. Migration `084_create_document_templates.sql` seeds the factory descriptor for every existing company.

**Descriptor (v1):** `{ schema_version, brand, theme, sections[], footer }`. Free-text fields (`terms.body_md`, `footer.text_md`) use a CommonMark Markdown subset. Validation is enforced by an inline JSON-Schema validator (`backend/src/services/documentTemplates/validator.js`) — no new runtime dependency.

**Renderer wiring:** `estimatePdfService.renderEstimatePdf(estimate, descriptor)` reads brand/theme/sections from the descriptor, falling back to the frozen factory in `services/documentTemplates/factory.js` when none is supplied. `estimatesService.generatePdf` calls `documentTemplatesService.resolveTemplate(companyId, 'estimate')` for every PDF request. Renderer adapters are pluggable via `services/documentTemplates/rendererRegistry.js` (`register(documentType, adapter)`).

**API:** `/api/document-templates` (list, get `:id`, PUT `:id`, POST `:id/reset`, POST `:id/preview`, GET `factory/:document_type`). Mounted with `authenticate, requirePermission('tenant.integrations.manage'), requireCompanyAccess` (P0 reuses the integrations permission until a dedicated `tenant.documents.manage` is seeded).

**Settings UI:** `/settings/document-templates` (list grouped by `document_type`) and `/settings/document-templates/:id` (form editor: Brand, ACH, Theme color pickers, Section visibility toggles, Terms & Warranty Markdown textarea, Reset to default).

---

## Sales CRM MCP

Sales CRM MCP adds a backend CRM core plus MCP-compatible tool access for seller workflows.

**Storage:** `crm_accounts`, `crm_deals`, `crm_deal_contacts`, `crm_deal_history`, `crm_activities`, `crm_notes`, CRM metadata tables, sales task links, and optional `crm_pipeline_weekly_snapshots`.

**API:** `/api/crm` is mounted with authenticated company access and provides account/contact/deal cards, pipeline, activities, tasks, notes, metadata, predefined lists, and allowlisted writes. Direct entity access is tenant-scoped.

**MCP:** `/api/crm/mcp`, `/mcp/crm`, legacy SSE, and stdio expose the same tool registry and executor. Tools are marked `read` or `write`; write tools require `sales.crm.write` plus confirmation. Typed write tools are limited to the allowed deal fields and `task.status`, return before/after values, generate or propagate request id, and write audit. Bulk/delete tools are not registered.

**Analytics:** pipeline/forecast tools support owner/team/period scope, stage and forecast grouping, total and weighted pipeline, commit/best case/pipeline totals, changes, risky deals, and slippage. Current truth is `crm_deals`; change truth is `crm_deal_history`; snapshots are optional comparison baselines.

**Sales Workflows:** `crm.list_sales_workflows` discovers ready-made workflow selections. `crm.get_sales_list` and explicit alias tools cover my open deals, closing this month/quarter, deals without activity, deals without next step, risky deals, top accounts by pipeline, accounts needing follow-up, contacts missing role/title/email, and tasks due this week.

**Rollout:** CRM REST and authenticated MCP are mounted behind `authenticate, requireCompanyAccess`; public MCP is token-gated and fail-closed by env config. Regression tests cover auth/tenant gates, tenant isolation, write allowlist/audit, no delete tools, secret redaction, slippage/history, stale activity, and workflow lists.

---

## Slot Recommendation Picker (SLOT-ENGINE-001)

When the slot-engine marketplace app is installed, the new-job slot picker (`CustomTimeModal`) surfaces engine recommendations as a side panel of cards. The standalone `slot-engine` service ranks candidate technician/time windows; each recommendation carries a `score`, a `confidence` tier (`high`/`medium`/`low`), an `explanation` string, and a `requires_dispatch_confirmation` flag. The engine I/O contract and ranking are unchanged by the UX polish — only how these signals are presented.

**Trust signal — temperature mini-bar.** A recommendation's quality is shown as a single thin vertical "temperature" mini-bar on the card edge: fill height scales with `score`, color maps to confidence tier (`high` → green / "Best match", `medium` → blue / "Good fit", `low` → amber-muted / "Worth a look"). The raw numeric score is off the card face, available only via the card `title`/`aria-label` for dispatchers. The earlier separate raw-score and confidence chips are gone.

**Vocabulary.** Engine recommendations use "Recommended" consistently — the panel header reads "Recommended times" and the engine tech-bar pill reads "Recommended". The pill for a technician preselected from a duplicate-job copy reads "Preselected" (distinct from engine recommendations).

**Explanation copy.** The card sub-text is a terse English reason composed by the engine's `explain(m)` from candidate metrics (e.g. "Tech already working nearby · little extra driving · comfortable schedule gap"), with the fallback "Good fit for this route". It contains no date/time/technician (the card already renders those) and no machine tokens — `snake_case` reason codes never leak to the UI. An "Approx. address — confirm" amber flag appears only when `requires_dispatch_confirmation` is set.

**Empty state.** When the app is enabled and the engine is reachable but returns zero recommendations, the panel shows "No nearby openings — try another day" rather than vanishing. The existing graceful degradation is preserved: when the app is disabled or the engine is unreachable, the panel stays absent and the modal is unchanged. The picker is new-job only; reschedule/edit mode does not render the panel or temperature bar.

---

## On-the-way ETA notification (ONWAY-001)

From a Job card in a pre-visit status (`Submitted` or `Rescheduled`), a technician or dispatcher with `messages.send` taps a primary **"On the way"** CTA. A modal does one device geolocation, optionally computes a Google travel-time ETA (device coords → job service address, via `routeDistanceService`), and offers preset minute tiles (10/15/20/30/45/60) plus a custom 1–600 entry. On **"Notify client"** Albusto sends an outbound SMS to the customer ("Hi! Your technician {tech} from {company} is on the way and should arrive in about {eta} minutes.") and **then** advances the job.

**New Job status — `On the way`.** A new non-terminal Job status (color `#0EA5E9`) joins the existing set (`Submitted`, `Waiting for parts`, `Follow Up with Client`, `Visit completed`, `Job is Done`, `Rescheduled`, `Canceled`). It is reachable **into** from `Submitted`/`Rescheduled` and **out** to `Visit completed`/`Canceled`. It has **no Zenbooker outbound mapping** and is orthogonal to the Zenbooker `en-route` substatus (`markEnroute`). The Job FSM is dual-sourced (hardcoded fallback in `jobsService.js` + per-company published SCXML); the status is injected idempotently into every company's active machine by migration `127_job_fsm_on_the_way.sql`, with mirrors in `fsm/job.scxml`, the `073` seed, and the fallback maps. Applying migration 127 on prod is a deploy prerequisite.

**Endpoints** (on the existing jobs router, both `requirePermission('messages.send')`, `company_id` from `req.companyFilter` only):
- `POST /api/jobs/:id/eta/estimate { origin:{lat,lng} }` → `{ eta_minutes|null }` — pure read; `null` when origin/address/Google key is missing.
- `POST /api/jobs/:id/eta/notify { eta_minutes }` → resolves customer phone (422 `NO_PHONE`) and a sending DID (MRU `sms_conversations` → `SOFTPHONE_CALLER_ID`, 422 `NO_PROXY`), then sends via `conversationsService.sendMessage` (wallet-gated; recorded to the customer timeline as outbound), then sets status. Ordering is **SMS first, status best-effort**: a failed/wallet-blocked SMS leaves status unchanged; a status failure after a sent SMS returns `{ ok:true, warning:'status_not_advanced' }` without rolling back the SMS.

The outbound ETA SMS reuses the canonical Conversations send path (the single wallet cost-enforcement point) and the timeline write; no new SSE event is added (the standard `job.status_changed` and conversation write drive existing real-time refresh).

---

## Email in Pulse (EMAIL-TIMELINE-001 → EMAIL-OUTBOUND-001 → EMAIL-HTML-RENDER-001 → EMAIL-QUOTE-STRIP-001 → GMAIL-PUSH-FIX-001)

Email is a first-class channel of the contact timeline, on a `MailProvider` seam (Gmail live via the `google-email` marketplace app, IMAP-ready). Inbound arrives in real time through Gmail Pub/Sub push (GMAIL-PUSH-FIX-001), deduped by `provider_message_id`, with the fallback poll a true ~1-min backstop; outbound is sent from the Pulse composer, the standalone `/email` workspace, or Gmail itself — all three paths persist a contact link on the message (`email_messages.contact_id` / `on_timeline`, mig 129), so the timeline detail shows the thread regardless of who wrote first.

**Real-time delivery (GMAIL-PUSH-FIX-001).** A Gmail push carries a `historyId` that already includes the triggering message, so `GmailProvider.handlePushNotification` returns `cursor: null` and ingestion walks from the stored poll-maintained `mailbox.history_id` (new message in-window) instead of from the push's own `historyId` (which made `history.list` empty and wasted the push) — the email is ingested and contact-linked in seconds. The fallback poll is a genuine backstop at `EMAIL_SYNC_INTERVAL_MS` (~1 min): `listDueMailboxes` cadence keys off the last sync FINISH and honors the interval, blocking overlap only for a genuinely in-flight sync (10-min stuck-escape retained), replacing the old hardcoded ~10-min throttle that had let a single inbound email wait up to ~571s. A `[EmailPush] push handled` success log makes push delivery observable (its absence had caused a false "push is dead" misdiagnosis). Backend-only, no migration, no Google Cloud change.

**HTML rendering (EMAIL-HTML-RENDER-001).** An inbound email body in the timeline now renders as sanitized, style-isolated HTML (clickable links, buttons-as-links, tables, author styling) instead of plain text. Rendering goes through a shared `SafeEmailHtml` wrapper — an open Shadow DOM (two-way CSS isolation, no `all:initial` reset so author fonts/colors win) fed by a single app-wide hardened DOMPurify config (`sanitizeEmailHtml.ts`): DOMPurify defaults strip `<script>`/inline `on*`/`<iframe>`, plus `FORBID_TAGS` removes `<form>` and form controls (a phishing form is not passed through — the P0 caught in verification), the `afterSanitizeAttributes` hook forces `<a target="_blank" rel="noopener noreferrer">` and nulls `javascript:`/`data:` hrefs, remote and `cid:` images are blocked by default (per-email "Show images" toggle) while `data:` images render, `<style>` is preserved for shadow scoping, and any failure fails safe to `''` (never raw HTML). Plain-text and outbound bodies go through `linkifyToHtml` instead (escape-first, then link URLs/emails/phones — no new dependency); outbound never touches the sanitizer, and an inbound message with no `body_html` (or a fail-safe `''`) falls back to linkified plain text. The backend passes `body_html` RAW into the timeline email item (no server-side sanitizing, no migration); the `/email` workspace uses the same `SafeEmailHtml`, so exactly one DOMPurify config exists in the codebase. In the timeline (inbound only), the sanitized HTML then has the **quoted thread history stripped** so the bubble shows only the new reply (EMAIL-QUOTE-STRIP-001): a pure client DOM transform `stripEmailQuote.ts` runs after DOMPurify inside the `SafeEmailHtml` memo behind an opt-in `stripQuotedHistory` prop — it detects the quote boundary (`.gmail_quote` / `blockquote[type=cite]` / Outlook / `.yahoo_quoted` / guarded top-level `<blockquote>` / `On … wrote:` text), removes it plus its attribution line at the earliest/outermost cut, keeps in-message mid-body quotes, and falls back to the full body when the result would be near-empty (never blank, fail-safe to input, idempotent, no new dependency). The timeline turns the prop **on**; the `/email` workspace leaves it **off** and keeps the full thread.

**Unified by-contact list.** The Pulse sidebar is ONE timeline-rooted, offset-paginated SQL query (`timelinesQueries.getUnifiedTimelinePage`) unifying calls + SMS + email: `last_interaction_at = GREATEST(call, sms, email)`, 3-band sort (Action-Required → unread → recency) fully in SQL. Email threads surface in **both directions**: the inbound leg text-matches `from_email` against `contact_emails` (mig 143 functional index), the outbound-first leg reads only the persisted mig-129 link — recipient JSON is never expanded in the hot query (PULSE-PERF-001 discipline). One row per contact; the newest thread across both directions wins. An outbound-first thread (dispatcher writes an email-only lead first) orders by send time, shows the outbound icon (MailCheck), and is never unread — `unread_count` grows only on inbound; a reply flips the row to inbound + unread. Migration 155 idempotently backfills links for historical outbound sends (TO-only recipient match, drafts excluded via `message_id_header`).

**Mail Secretary** — AI triage of inbound email as a marketplace app (`mail-secretary`). Hooked into the ingest path (`emailTimelineService` → `mailAgentService.reviewInboundEmail`), it creates tasks for attention-needed emails and logs exactly one decision per email in `mail_agent_reviews` (verdict/category/confidence; re-pushed notifications are no-ops). Per-company `mail_agent_settings` (migs 152–153): enabled flag, confidence threshold, unknown-sender contact creation, owner assignment, line-based exclusion rules — managed on the app's settings page, gated `tenant.integrations.manage`. It stores Gmail message references and derived triage results, never raw email bodies.

**Classifier provider (MAIL-LOCAL-LLM-001).** Triage classification runs on a **local LLM by default** — Ollama `qwen2.5:14b` on the `mini` host — through a thin provider dispatcher in `mailAgentClassifier.js` selected by `MAIL_AGENT_PROVIDER` (`ollama` default | `gemini`); the prior Gemini call is retained verbatim as a dormant one-env revert. The prompt, verdict parser, and failure semantics (a throw makes the caller write `verdict='error'` and create no task) are byte-identical across providers — only the transport changed — so mail triage no longer shares the Gemini spend cap and email content stays on-perimeter. **Call summaries remain on Gemini.** New env: `MAIL_AGENT_OLLAMA_URL` (default `http://127.0.0.1:11434`), `MAIL_AGENT_OLLAMA_MODEL` (default `qwen2.5:14b`), with `MAIL_AGENT_TIMEOUT_MS` default raised to 60000. Reaching the local model from prod is a deploy prerequisite: the `mini`'s Ollama is localhost-only today (needs `OLLAMA_HOST=0.0.0.0` + Tailscale/tunnel + the model pulled).

The same exclusion rules also govern **email-channel visibility in Pulse (MAIL-MUTE-001)** — but only `from:`-scoped rules (subject/body rules stay task-only). A muted sender's inbound email no longer links onto the contact timeline (`linkInboundMessage` early-returns `{skipped:'muted_sender'}`, so no unread flip, no auto-created contact) and no longer surfaces the contact in the unified by-contact list: `getUnifiedTimelinePage` takes optional `mutedEmails`/`mutedDomains` (derived per-company by `mailAgentService.getMutedSenderSet` from literal, non-negated `from:` tokens and passed in by `GET /api/calls/by-contact`) and gates the five email terms with `AND NOT email_muted`. The mute is **channel-specific** — the same contact's inbound calls and SMS still surface and bump the row — and fully reversible by removing the rule (the muted set is recomputed each request). It is migration-free (empty set ⇒ no behavior change for other callers) and fail-open (a settings/parse error yields an empty set, never suppressing more than intended). No new UI, field, or endpoint — `exclusion_rules` remains the single source of truth.

## Tasks nav badge (TASKS-COUNT-BADGE-001)

The Tasks nav item carries a live count badge of OPEN tasks visible to the current user — identical visibility to `GET /api/tasks` (managers with `tasks.manage` see the whole company; everyone else only their own). Served by `GET /api/tasks/count` (which shares `buildTaskListFilters` with `listTasks`, so count and list can never drift), refreshed on mount/route-change, a 60s poll, and a PII-free `task.changed` SSE ping (`{company_id}` only — the client refetches the server-scoped count, never computes it). Mirrors the Leads new-count badge.

## Contact email merge (CONTACT-EMAIL-MERGE-001)

Adding an email to a contact (multi-email list in the contact editor) merges any existing correspondence for that address into the contact — the email analogue of the phone-number merge. Inbox-only messages link onto the contact timeline; the whole PATCH is one transaction (a merge failure rolls back the email add); the merge is company-scoped and refuses cross-tenant. The originally-silent "address owned by another contact" branches (auto-merge of an email-only auto-contact / auto-re-point) were replaced by the user-confirmed flow of CONTACT-MERGE-001 below; only the inbox-only linking stays silent.

## Contact merge with confirmation (CONTACT-MERGE-001)

Adding a phone or email that already belongs to ANOTHER contact of the same company is no longer resolved silently: `PATCH /api/contacts/:id` detects the conflict at the top of its transaction (before any write, `FOR UPDATE` on target + owner), rolls back and returns `409 CONTACT_ATTRIBUTE_CONFLICT` with both contacts' full composition. The client (both v1 editors — `EditContactDialog` and the Pulse panel's inline email editor — via a shared `useContactConflictFlow` hook) shows a two-column `MergeContactsDialog` offering **Merge contacts** (full merge: survivor = the edited contact, its fields win, its ZB linkage kept, calls/SMS/emails/leads/tasks move, dup deleted), **Transfer phone/email** (only that attribute + its thread moves; hidden when the donor would be left with no phone and no email), or **Cancel** (nothing was ever committed). Confirmation is a strict-echo `resolutions[]` retry of the same PATCH, re-validated in-tx, so a stale choice can never execute. This also covers the previously-uncovered phone side and closes the scalar-`email` hole server-side (Decision E): a bare `{email}` PATCH now persists `contact_emails` and passes conflict detection for every client of the route. No migration; background ingestion paths never see a dialog.

## Email-only Pulse timelines (EMAIL-LEAD-ORIGIN-001)

A contact with an email but no phone (e.g. an email-auto-created contact) is now first-class in Pulse: the detail card renders on identity (`p.phone || p.contact?.id`), not phone; `PulseContactPanel` is phone-null-safe and carries a "+ Create Lead" action; a lead can be created from such a contact (phone optional — `POST /api/leads` accepts phone|email|contact_id origin; email-origin leads are lead-only since ZB job creation needs a phone). The card detects an existing open lead via `getLeadByContact` (mirrors `getLeadByPhone`, incl. the contact-has-job filter) so it shows the lead instead of offering a duplicate. The hot unified-list query is untouched (email-origin leads surface via the email signal + Leads page).

## Voice-agent slot recommendation (VAPI-SLOT-ENGINE-001)

The VAPI voice agent (Sara) offers engine-ranked arrival windows live on the call and turns the caller's pick into a schedule-blocking hold. A gated, safe-fail VAPI tool `recommendSlots` (in `backend/src/routes/vapi-tools.js`, behind `x-vapi-secret` + the `smart-slot-engine` marketplace gate, company hardwired to `DEFAULT_COMPANY_ID`) calls `slotEngineService.getRecommendations` directly and returns ≤3 keyed windows (`date|start|end`), with deeper-search via `excludeSlots`/`daysAhead`; any fault (app off, engine down, empty, throw) degrades to `{available:false,fallback:true}` at HTTP 200 so a live call is never crashed. On acceptance, `handleCreateLead` persists the chosen slot onto the lead — `LeadDateTime`/`LeadEndDateTime` (via `tzCombine`, a thin adapter over `companyTime.dateInTZ`) plus `Latitude`/`Longitude` — with no new entity and no migration; the lead schema already carries these columns. The held lead then occupies the engine's area-schedule: `slotEngineService.buildScheduledJobs` emits open, geo-located, in-window leads as occupancy so the same window is not re-offered, and both the occupancy sub-read and the Schedule UNION filter terminal status case-insensitively (`LOWER(status) NOT IN ('converted','lost','spam')`) so a dispatcher converting the lead→job or losing/cancelling it frees the slot. The live assistant config (`voice-agent/assistants/lead-qualifier-v2.json`) gains the tool + prompt in-repo, but pushing it to the live VAPI assistant is a separate owner-gated step.

### Nearest-tech distance fallback (SLOT-ENGINE-NEAREST-FALLBACK-001)

The engine's routing radius was tighter than the service area, so a caller who was in-area but had no technician within the normal radius (e.g. Weston MA, nearest tech ~11.8 mi vs a 10-mi gate) got zero engine recommendations and the voice agent fell back to generic windows. The engine now runs **two passes**. **Tier-1** is today's code path, byte-identical (proven by a differential of the pre-change engine against a captured baseline across covered inputs). **Tier-2** fires *only* when Tier-1 dedupes to zero candidates: the same candidate loop re-runs on a **clone** of the config (`deriveFallbackConfig`, never mutating the shared `DEFAULT_CONFIG`) with the distance ceilings and edge/extra-travel caps lifted to a fixed **`fallback_max_distance_miles` = 25** and `allow_empty_day_candidates = true`. It offers the nearest technician (min of base-distance and nearest-existing-job-distance), never relaxes overlap (still 0) or feasibility, keeps a free tech's empty-day windows anchored at their base, and ranks nearest-first via the existing distance score. Tier-2 recs are tagged `fallback_tier: 2` + reason `nearest_tech_fallback`, and `summary.used_nearest_fallback` records which pass produced the offer. The 25-mile cap is a hard ceiling (24.9 mi rescued, 25.0 mi not). Both the engine default (`slot-engine/src/config.js`) and the CRM-built override (`buildConfigOverride` in `slotEngineSettingsService.js` — the live geography source) emit the key; it is a fixed constant, so there is no new company setting, no migration, and no change to Sara/VAPI or the `recommendSlots` tool. The truly-out-of-area contract is unchanged (a 30-mi caller still gets no window).

## Agent-agnostic CRM skill layer + existing-customer skills (AGENT-SKILLS-001)

The CRM exposes its customer-service capabilities as a provider-neutral **skill layer** so the voice agent is swappable: all logic lives in `backend/src/services/agentSkills/`, and every agent (the VAPI voice adapter, an MCP client, any future agent) is a thin adapter over the same `runSkill(name, companyId, ctx, input)` choke-point. There is no business logic in an adapter. Fourteen skills are registered — the five relocated legacy VAPI tools at L0, plus nine existing-customer skills: `identifyCaller` (L0, branches the call), `getCustomerOverview`/`getJobStatus`/`getAppointments` (L1 reads), `getJobHistory`/`getEstimateSummary`/`getInvoiceSummary` (L2 sensitive reads), and `rescheduleAppointment`/`cancelAppointment` (L2 writes).

Verification is a single server-side gate (`verificationGate`): the level is re-derived from the DB every call from the identity block (phone/name/zip/street/contactId only), so a client- or LLM-asserted `verified` flag is structurally ignored — L1 needs a phone match to exactly one company-scoped contact, L2 additionally needs a confirmed name (≥2 tokens) and a ZIP/street match. `identityResolver` resolves across leads + contacts + jobs (covering the case where an existing customer's lead row is suppressed once a job exists). Reads below L2 disclose nothing sensitive (address is confirm-only, no amounts, no line items); writes and sensitive reads ownership-pre-check `getJobById(jobId, companyId)` + the verified `contactId` before any mutation or disclosure, keeping tenant isolation absolute even though some getters take a bare id. No payment is ever captured by voice.

Two adapters ship: the refactored `vapi-tools.js` (a thin generic dispatch, `DEFAULT_COMPANY_ID` for the VAPI transport; legacy tools stay byte-identical, verified by a golden capture), and a parallel `svc.*` MCP triplet (`agentSkillsMcp*` + routes/stdio, serverInfo `albusto-service-crm-mcp`) that reuses the sales `crmMcp*` validator/response as-is and leaves the sales stack untouched — tenant comes only from context, the framework write/confirmation gate composes as an outer gate over the inner L2, and public writes are off by default. Reschedule/cancel write the Albusto schedule and push Zenbooker (`rescheduleItem` gained the ZB write-through; the local write stays authoritative and a ZB failure reconciles-then-409 rather than diverging), with an "AI Phone" audit note on every write. No migration. Deploying the backend makes the skills callable; Sara only *uses* the new existing-customer skills once her assistant config declares them (new tool defs + prompt branching) and is pushed live — a separate owner-gated step.

**AGENT-SKILLS-002** refined this from live call experience. Identity resolves a multi-contact-on-one-phone deterministically to the most-recent contact (`created_at` DESC), preferring a name/ZIP match when given, instead of stalling on ambiguity; `ambiguous` now survives only on the no-phone name path. Verification was relaxed: an *identified* caller (by phone, or by name+ZIP when the number is masked/insurance) is served the existing-customer skills at **L1** — no separate name+ZIP re-confirmation — because in this business the appointment/estimate state isn't treated as sensitive. The non-negotiables are unchanged and were re-proven: company isolation, the per-`contactId` ownership pre-check on every write and sensitive read, cancel retention, no card by voice, and L0 (unidentified) still refused. A new `bookOnLead` skill (L1 write) surfaces and books onto a customer's *existing* open lead — the case where someone submitted a request or an insurer sent their info and they call before a dispatcher opens it — writing the chosen slot as a schedule-blocking hold on that lead via `updateLead` (never a duplicate; no `status` change, so no FSM churn), with `getCustomerOverview`/`getJobStatus` now surfacing an open lead's status + proposed window. Owner-accepted tradeoffs: a shared-phone caller is served the most-recent contact's own (company-scoped) data, and financial summaries are now L1 (reversible with a two-line registry revert).

## Outbound parts-visit scheduler + typed task-actions (OUTBOUND-PARTS-CALL-001)

When a job reaches the new **`Part arrived`** blanc-status (FSM mig 156), the CRM auto-creates a `kind='part_arrived_call'` task carrying two **typed task-actions** — a reusable pattern where a task's `actions` jsonb column (mig 157) drives backend-executed buttons resolved through a closed server-side registry (`POST /api/tasks/:id/actions/:type`, `tasks.manage`, company-scoped); `TaskCard.tsx` renders one button per `actions[]` entry beside the standard Done/Cancel, with no per-feature hardcoding. The **robot_call** action pre-computes a slot (`recommendSlots`) and enqueues an `outbound_call_attempts` row (mig 158, partial-unique `(job_id)` so at most one live call per job); no slot / engine fault / missing phone means it does **not** dial and writes a dispatcher-actionable reason instead. An env-gated worker (`FEATURE_OUTBOUND_CALL_WORKER`, business-hours clamp in the company tz, `immediate/+2h/next-morning ×3` backoff, `FOR UPDATE SKIP LOCKED` claim) places the call via VAPI `/call` with context in `assistantOverrides.variableValues`. A new outbound assistant (`parts-visit-scheduler.json` — no name/address re-verification, no warranty line) offers the window; acceptance runs the `confirmPartsVisit` skill (L0 on the outbound surface, in-skill ownership pre-check `job.contact_id === bound contactId`), which reschedules **the same job** (ZB write-through) → `Rescheduled`, adds an AI-Phone note, and closes the task — never creating a new lead/job. Call outcomes are classified by a secret-auth webhook (`POST /api/vapi/call-status`) whose company is read from the correlated attempt row, never from the body (anti-spoof). The **manual_call** action routes to the desktop softphone (`openDialer`) or a native `tel:` on mobile. Inbound Sara / `vapi-tools` are byte-identical (variableValues are a no-op without `assistantOverrides`); v1 is gated to Boston Masters. Verified by 87 jest tests + a real-DB harness (`scripts/verify-outbound-parts-call-001.js`, 13/13). **Dispatchers trigger the outbound scheduling call from typed task-action buttons** rendered by a shared `TaskActionButtons` component on **both** the Job-card task stack **and** the Pulse **Action Required** banner (🤖 robot_call opens the slot-picker described under SLOTPICK-001 below; 📞 manual_call dials with no confirm; the row self-gates on `tasks.manage` and hides while snoozed) — the **OUTBOUND-PARTS-CALL-BTN-001** follow-up fixed the `SELECT_TASK` read projection that had been dropping the `actions` jsonb column so the buttons rendered nowhere, and best-effort (non-fatal) thread-links the part-arrived task to the contact's timeline so it also surfaces in Pulse AR. Owner-gated deploy (migs 156–159, VAPI env, assistant push). **OUTBOUND-PARTS-CALL-SLOTPICK-001** then made the robot's offered window an explicit dispatcher choice: 🤖 opens the **reused reschedule form** `CustomTimeModal` (recommendations + technician timelines + map; only the header "Schedule the robot call" and CTA "Queue robot call" differ), where the dispatcher must pick the exact appointment window — a ranked recommendation (top pre-selected) or a manual timeline / custom-time pick, with no-recs/engine-off not blocking — which is then offered to the customer **verbatim** on the call. The modal emits an ISO window; the server converts it to the company-tz `slot_json` and validates it (`partsCallService.buildRobotCallSlot` — parse, `start<end`, same local day, `[today, today+60d]`), surfacing a bad window as a live **400** in the modal; `startRobotCall` gained a 5th `dispatcherSlot` arg that skips `recommendSlots` (the no-slot auto-compute path unchanged), and the Pulse `open_task` now carries `parent_id`/`parent_type` so the AR-banner button resolves the job. Reused `CustomTimeModal` via additive `title?`/`confirmLabel?` props (reschedule/new-job callers byte-identical); no migration; Reviewer-APPROVED, not yet deployed. (Known follow-up: `buildRobotCallSlot` derives the tz from `dispatch_settings.timezone` while the modal shows `company.timezone` — identical for the current tenant and shared with the auto-compute path, but divergent per-tenant settings could shift the queued window vs. what the dispatcher saw.) **OUTBOUND-PARTS-CALL-TECHSLOT-001** then made the flow **technician-scoped** without touching the slot-engine — all input-shaping lives in the backend proxy `slotEngineService`: an optional `new_job.technician_id` filters the company roster to that ONE technician (unknown id → empty roster → safe-fail) and a single-tech request widens the query-scoped ranking caps (`top_n`≥5, per-technician/per-timeframe = 5 via `config_override` merge) so the tech's full same-day window set returns; the no-tech legacy path is byte-identical. **Jobs with 2+ assigned technicians are excluded from robot calls** end-to-end: a non-bypassable server gate in `startRobotCall` returns `{ok:false, reason:'multi_tech'}` (200-envelope, no attempt INSERT) and `RobotCallSlotModal` renders a "please call manually" panel instead of the picker. Otherwise the picked lane's `techId` (dispatcher pick > sole-tech default > null) plus the job's `lat`/`lng` ride in `slot_json` and are injected into the call's `variableValues`, server-constraining the in-call `recommendSlots` to that technician — the model cannot override it (`technicianId` is deliberately absent from the assistant tool schema). The skill also handles **in-call day/time requests**: `targetDay` (`YYYY-MM-DD` → earliest=latest) offers that day's windows, and `targetDay`+`targetTime` (`HH:MM`) returns EXACTLY ONE window (containment → dist 0, else nearest start, tie → earlier); invalid formats are ignored, every fault still safe-fails. On the CRM side, Job-card reschedule recommendations scope to the job's current technician (deterministic first by stable id sort; timelines still show all techs) via an additive `recommendTechId?` prop on `CustomTimeModal` → `technician_id` in the recs request, and a REQ-3 guard keeps a 2+-tech reschedule time-only (payload omits `tech_id`, both techs stay assigned). The repo half of the assistant update (`parts-visit-scheduler.json` += `targetDay`/`targetTime` + prompt guidance) is committed; the live VAPI assistant PATCH is a deploy-time manual step. No engine change, no migration; 294 backend tests green (14 suites incl. red→green negative controls) + frontend build green; Reviewer-APPROVED, not yet deployed. (Non-gating notes: `JobInfoSections`' initial-slot highlight uses the unsorted first tech while the recs scope uses the sorted first — a cosmetic lane-highlight mismatch is possible on multi-tech jobs; the ranking-cap widen also applies to desktop single-tech recommendations, consistent with the spec.)

**OUTBOUND-CALL-TIMELINE-001** then made those robot calls visible in the Pulse call timeline the way softphone calls already are — no migration, no new route or SSE name. The moment the worker stamps a `vapi_call_id`, a non-fatal `vapiCallTimelineService.recordPlacement` mirrors the placed call into `calls` as an in-progress row keyed by a **synthetic sid `vapi:<vapiCallId>`** (`direction='outbound'`, `status='initiated'`, `answered_by='ai'`, thread resolved by the dialed phone, company taken **only** from the attempt row) and publishes the standard `call.updated` SSE. VAPI webhooks then drive it live: a previously-dropped `status-update` maps to a mid-call transition (`applyStatusUpdate`), and `end-of-call-report` runs `finalizeFromEndOfCallReport` — both placed **before** the attempt FSM/idempotence guard (repeat-webhook safe) and both wrapped non-fatally (the webhook still 200s, the retry FSM and its note texts are byte-identical). Finalize **re-keys the synthetic sid to the real Twilio `CA…`** as soon as `phoneCallProviderId` appears (a 23505 clash with a cold-reconcile real-sid row → merge-and-delete), maps `endedReason` → call status (voicemail/no-answer/busy/completed-if-duration/failed), then writes the child transcript (VAPI summary → `raw_payload.gemini_summary`) and recording (`recording_url`, `source:'vapi'`) rows — children are written **only after** the sid resolves (FKs). Two reconcile guards keep a live call alive: the 5-min stale sweep and the hot non-final poll gain `AND call_sid LIKE 'CA%'` so a `vapi:%` row is never Twilio-polled → 404 → killed mid-call, and a separate 15-min synthetic sweeper finalizes a genuinely-lost `vapi:%` row (`failed`, monotonic, company-scoped). The recording-playback proxy (`GET /:sid/recording.mp3`) streams from `recordings.recording_url` (the VAPI S3 URL, no auth headers) when the sid isn't a Twilio `RE…`; the Twilio branch is byte-identical, so inbound / softphone / Sara playback is unaffected. On the thread-feed tile a shared `isAiAnsweredBy` predicate renders a Bot chip (inbound Sara is marked too, intended). The live OUTBOUND assistant's `serverMessages` must gain `status-update` (and carry artifact/recording/transcript/summary on end-of-call) at deploy — a manual VAPI PATCH with the repo assistant JSON synced to match; without it `status-update` simply never fires (graceful degrade — end-of-call still finalizes). 107 backend tests across 6 suites + frontend build green; Reviewer-APPROVED, not yet deployed. (Pre-existing non-gating note: the recording proxy has no per-call company-ownership SQL scope — identical on the Twilio branch, with the tenant check enforced by the router's GET guard.)

## Native iOS field-tech app (MOBILE-TECH-APP-001 → 002)

A native iOS app for the `provider` role (field technician) lives in a **separate repo, `albusto-mobile`** (RN + Expo dev-client, iOS only); this repo owns only its backend contract. **v1 (M00–M11):** offline **read-only** core — a local SQLite jobs cache drawn instantly on screen, silently refreshed by a provider-scoped delta sync (`GET /api/sync/jobs`, forward `(updated_at,id)` cursor, `changed/unassigned/tombstones`, 30-day full-sync window); Keycloak PKCE auth (public client `crm-mobile`), schedule/agenda, job detail, status FSM (en-route/start/complete), notes + photos, Apple Maps navigation, `tel:` calls (softphone stays desktop-only — MOBILE-NO-SOFTPHONE-001), APNs push. Backend prerequisites (Phase 0, migs 150 `job_tombstones` + 151 `device_tokens`, `routes/sync.js`, `routes/devices.js`, `pushService.sendToUser`) are deployed to prod; there is **no offline write queue** — every write requires network.

**002 — tech-workflow parity (mobile-repo only).** Three additions bring the app to what a provider already does in the mobile web CRM: **(a) Finance on the job card** — an "Estimates & Invoices" section in JobDetail with document detail view, editor-first create/edit (POST only on first Save), a read-only Price Book picker (Category → Group → Item; a Group tap bulk-adds its member lines via `groups/:id/expand`) plus freeform lines, and send via email/SMS (SEND-DOC-001 routes; 409/422/402 map to plain "ask the office" alerts). Invoice item saves follow the INVOICE-EDIT-ITEMS-001 contract exactly via a dirty-flag payload builder: items untouched ⇒ no `items` key, touched ⇒ full array, emptied ⇒ `[]`. **(b) Tasks tab** — list/complete/create of the tech's OWN tasks; scoping is 100% server-side (`scopeOwnerId` — the app sends no owner filter, ever), complete is optimistic with revert, the composer pins the current job (from JobDetail) or picks one of the tech's cached jobs, and the tab badge reads `GET /api/tasks/count` fail-silent. **(c) Two-tier search** — instant local search over the SQLite cache (works offline) plus debounced server search (`GET /api/jobs?search=`, provider-scoped, finds jobs outside the cache window; contacts search → `tel:` call), deduped latest-wins; opening a server hit uses a new JobDetail online cache-miss fallback (`GET /api/jobs/:id` into component state — **never written to SQLite**).

**Boundaries.** All 002 surfaces are **online-only** (owner decision D1): finance, tasks, and server search never enter the SQLite cache or the sync delta; offline they render a needs-connection placeholder while the v1 cached read path keeps working. Out of scope: any payment collection (record-payment, Tap-to-Pay — **v1.5/M12**), CallKit VoIP (**v2**), Android, company-wide document list screens, Price Book editing, other users' tasks. **The backend contract is frozen and unchanged:** 002 shipped with zero backend diffs and zero migrations (max stays 155) — the app only consumes existing routes (estimates/invoices, price-book reads, tasks, `jobs?search`/`jobs/:id`, `contacts?search`) behind existing provider permissions (migs 050/136/138/141), and the protected mobile surface (`sync.js`, `devices.js`, jobs status routes, `keycloakAuth.js`) is unchanged since the Phase-0 deploy. Security is server-side scoping only (`getProviderScope`, `scopeOwnerId`, `requirePermission`) — the app implements no client-side role logic.

## Call flow: dispatchers busy → voice assistant (CALLFLOW-BUSY-TO-AGENT-001)

When every dispatcher in the target group is offline or on a call, an inbound call no longer parks on the voicemail announcement — it proceeds along the flow to the voice assistant. This was a **data-only** change to the company's active flow graph (`call_flows`, editor format): the queue node's fallback edge (`queue.timeout queue.not_answered queue.failed` — covering instant no-agents, 25s ring-timeout, and dial failure) was repointed from the Voicemail node to a dedicated `vapi_agent` node ("AI Backup", same Sara), whose own failure edge lands on the business-hours voicemail as the last resort. The runtime needed no changes: `availableAgentsForGroup` already filters offline + busy, the no-agents path already follows the failure edge instantly (no ringing), and dial-action responses carry the next node's TwiML inline, so the caller is seamlessly bridged to the assistant (`answerOnBridge` preserved). The transform ships as a pure function + idempotent guarded CLI (`scripts/apply-callflow-busy-to-agent-001.js`: dry-run default with a rollback payload, FOR-UPDATE single-row apply, P1–P6 shape refusals) so the graph stays editable in the flow builder (all fields inside the editor whitelist; `ensureFlowForGroup` regenerates only empty graphs and cannot clobber it). After-hours branch untouched.

## Mobile Schedule map (SCHEDULE-MOBILE-MAP-001)

The mobile Schedule day view gains a map of the currently-listed jobs. A single toggle icon-button (next to the filter/gear in `MobileScheduleBar`) swaps the list for a full-screen map and back — a Map icon in list mode, a List icon in map mode. The map (`ScheduleJobsMap`) is presentational: it takes the already day-scoped, tech-filtered `scheduledItems` as a prop (no new fetch), groups jobs by technician, numbers each tech's stops 1..N in start-time (route) order, and plots them as numbered pins colored by the same `getProviderColor` key the schedule tiles use, with straight per-tech connector polylines (no paid Directions), an InfoWindow on tap, and a bounds fit. Jobs without a successful geocode are excluded and counted; a missing key or an empty day show inline fallbacks. It reuses the slot picker's numbered-pin renderer (`makePinSvg`, extracted to `utils/mapPins.ts`). Mobile-only and gated by `isMobile && mobileMapOpen`; the desktop schedule and the live slot picker are behaviorally unchanged. Frontend-only — jobs already carry `lat`/`lng` (SCHED-ROUTE-001), so no backend or migration.

## Installed web app / PWA on iOS (PWA-FIX-001)

Separate from the native app, the **web SPA** (`app.albusto.com`) is installable to the iOS home screen. It previously shipped with **no Web App Manifest** and no Apple standalone meta, so iOS had no `scope` and treated only the launch path as in-window — any full navigation (notably the cross-origin Keycloak login redirect) got kicked out of the standalone window into an `SFSafariViewController` overlay (close-X chrome, shifted safe-areas → apparent layout breakage). The fix is frontend-only: a `manifest.webmanifest` with **`scope:"/"`** (every route in-window), `display:standalone`, brand icons (an "A" letter-mark, 192/512/512-maskable + a 180 apple-touch-icon) and the Apple/PWA `<head>` meta (`apple-mobile-web-app-capable`, `viewport-fit=cover`, `theme-color #fffdf9`). The second eject trigger — `AuthProvider` calling a full `kc.login()` redirect on **any** token-refresh failure — is fixed by a pure `refreshPolicy` classifier: transient/offline failures retry with backoff (`[2s,5s,10s]`) and stay in-window; the full redirect fires only on a genuinely dead session. **Deploy prerequisite (owner):** the prod static server (Caddy) must serve `/manifest.webmanifest` as `application/manifest+json` and `/icons/*` as `image/png` **before** the SPA `index.html` catch-all, or iOS ignores the manifest. Frontend-only, no backend, no migration.
